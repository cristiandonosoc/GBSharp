<Window x:Class="GBSharp.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:GBSharp.View"
        xmlns:viewModel="clr-namespace:GBSharp.ViewModel;assembly=GBSharp.ViewModel"
        Title="MainWindow" Height="600" Width="800" WindowState="Maximized">
  <Window.Resources>
    <DataTemplate DataType="{x:Type viewModel:CartridgeViewModel}">
      <view:CartridgeView></view:CartridgeView>
    </DataTemplate>
  </Window.Resources>

  <Grid Style="{DynamicResource MainContentGrid}">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"></ColumnDefinition>
      <ColumnDefinition Width="*"></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <ContentPresenter Content="{Binding Cartridge}"/>
    <Grid Grid.Column="1" Style="{DynamicResource MainContentGrid}">
      <Image Source="Images/Nintendo_Gameboy.jpg"></Image>
      <Button HorizontalAlignment="Left" Margin="296,344,0,0" VerticalAlignment="Top" Width="44" Height="44">
        <Button.Template>
          <ControlTemplate TargetType="Button">
            <Grid>
              <Ellipse Stroke="Transparent" Fill="Transparent">
              </Ellipse>
              <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
            </Grid>
          </ControlTemplate>
        </Button.Template>
      </Button>
      <Button HorizontalAlignment="Left" Margin="242,369,0,0" VerticalAlignment="Top" Width="44" Height="44">
        <Button.Template>
          <ControlTemplate TargetType="{x:Type Button}">
            <Grid>
              <Ellipse Stroke="Transparent" Fill="Transparent"/>
              <ContentPresenter HorizontalAlignment="Center"
      					VerticalAlignment="Center"/>
            </Grid>
          </ControlTemplate>
        </Button.Template>
      </Button>
    </Grid>
  </Grid>
</Window>
